{
  "name": "grovemd",
  "version": "0.1.0",
  "description": "Grove — a local markdown wiki for any folder. Browse, read, and (soon) edit. Renders GFM, math, mermaid, syntax highlighting, and media previews.",
  "type": "module",
  "license": "MIT",
  "author": "Moriz Mensi-Klarbach",
  "homepage": "https://github.com/MorizMensi/grove#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MorizMensi/grove.git"
  },
  "bugs": {
    "url": "https://github.com/MorizMensi/grove/issues"
  },
  "keywords": [
    "markdown",
    "grove",
    "wiki",
    "viewer",
    "editor",
    "cli",
    "documentation",
    "notes",
    "obsidian-alternative",
    "file-browser",
    "angular",
    "express"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "grove": "./dist/server/bin/file-viewer.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build:frontend": "cd frontend && npx ng build --configuration production",
    "build:server": "tsc -p tsconfig.json",
    "build": "npm run build:frontend && npm run build:server",
    "build:wiki": "cd frontend && npx ng build --configuration wiki",
    "build:all": "npm run build && npm run build:wiki",
    "prepublishOnly": "npm run build:all"
  },
  "dependencies": {
    "express": "^5.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "~5.7.2"
  }
}
